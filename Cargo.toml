[package]
name = "ekf"
version = "0.1.0"
edition = "2024"

[features]
nalgebra-backend = ["dep:nalgebra"]

[dependencies]
nalgebra = { version = "0.33", optional = true }
num-traits = "0.2"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "backend_benchmark"
harness = false
